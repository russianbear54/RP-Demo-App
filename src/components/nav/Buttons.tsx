import React from "react";
import { Button, ButtonsDiv } from "../../styles/ListPage/styles";

// interface Props{
//   loading:boolean,
//   ships:array,
//   page:number
// }

// const ButtonsComponent:React.FC = ({loading,ships,page}) => {
//   return (
//     <ButtonsDiv>
//       <Button
//         disabled={!loading && page < 1}
//         onClick={() => {
//           setPage((prev) => prev - 1);
//         }}>
//         Back
//       </Button>
//       <Button
//         disabled={!loading && ships.length < 8}
//         onClick={() => {
//           setPage((prev) => prev + 1);
//         }}>
//         Load More
//       </Button>
//     </ButtonsDiv>
//   );
// };

// export default ButtonsComponent;
